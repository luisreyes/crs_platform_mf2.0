import React from 'react';
import ReactDOM from 'react-dom/client';
import Audits from './view/Audits'; 

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Audits />
  </React.StrictMode>,
);
